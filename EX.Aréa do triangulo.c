#include <stdio.h>
#include <stdlib.h>
#include <windows.h>


	
	int main() {
	float base, altura, area;
		printf("insiria o valor da BASE\n");
		scanf("%f", &base);
		printf("insiria o valor da ALTURA\n");
		scanf("%f", &altura);
		area = (base*altura) /2 ;
		printf("A area do triangulo = %0.2f", area);
		
  
  
	return(0);
}
