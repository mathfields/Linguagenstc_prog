#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	 int base, altura;
	 float area;
base = 10;
altura = 10;
area = (base*altura)/2;
 printf("A base do triangulo é %d * %d = %f",base,altura,area);
 
	return 0;
}
